
name: CI

on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -

      - name: Run tests
        run: poetry run pytest
